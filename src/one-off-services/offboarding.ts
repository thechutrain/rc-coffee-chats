/** function used for deactivating users, notifying them, updating maintainers that
 *
 * @param batchId
 */

export function offBoardUsers(batchId: number) {
  /**
   * - find all users in the given batch
   * - find all active users who are in the current batch
   * - deactivate all of these users
   * - notify all of the users they have been deactivated
   * - send this logging information to admins
   */
}
